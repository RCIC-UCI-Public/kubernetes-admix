!include common.yaml
---
- package: Kubeadm profile file 
  name: kube-profile 
  version: "{{ versions.k8s }}"
  scriptfile: kube-profile.sh 
  description: |
    Calicoctl for k8s {{version}}. 
  root: "/etc/profile.d"
